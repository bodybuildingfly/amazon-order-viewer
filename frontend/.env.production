# frontend/.env.production
# This file is used for the production build.
# An empty value means API calls will use relative paths (e.g., /api/login),
# which is correct for a single-server deployment.
REACT_APP_API_BASE_URL=
